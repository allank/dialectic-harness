#!/bin/sh
cat > "$DIALECTIC_OUTPUT_FILE" <<'EOF'
## Narrative

The rollback gap (C1, turn 2) was conceded immediately; the store choice (C2, turn 2) stayed contested to the round limit.

## Proposed Changes

- Add a rollback section to the artifact (C1, turn 2).

## Judgment Calls

- Store choice: latency budget vs justified design — decide which constraint wins (C2, turn 2).
EOF
